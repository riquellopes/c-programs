# include <stdio.h>

int main(){
    int num_one, num_two;

    printf("Please, enter with two number: ");
    scanf("%d %d", &num_one, &num_two);

    printf("You write: '%d and %d'\n", num_one, num_two);
}
